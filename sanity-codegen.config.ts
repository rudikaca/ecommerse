import { SanityCodegenConfig } from 'sanity-codegen';

const config: SanityCodegenConfig = {
    schemaPath: './sanity_ecommerce/schemas/schema.js',
    outputPath: './schema.ts',
};

export default config;
